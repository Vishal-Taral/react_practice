import Card from './card';
import './version.css'

let version = () => {
    return (
        <Card className="vesionDiv">
            <h3>the first version</h3>
        </Card>
    );
}

export default version; 